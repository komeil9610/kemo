{
  "version": 2,
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci --no-audit --fund=false --loglevel=error",
  "outputDirectory": "build",
  "functions": {
    "pages/api/**": {
      "maxDuration": 60
    }
  }
}

{
  "name": "rentit-edge-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute rentit_db --local --file=./migrations/0001_schema.sql",
    "db:migrate:remote": "wrangler d1 execute rentit_db --remote --file=./migrations/0001_schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.8.0"
  }
}
